import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  variantsContainer: {
    flex: 1
  },
  variantsText: {
    margin: 6
  },
  imageVariants: {
    width: 60,
    height: 60,
    margin: 10,
    borderRadius: 4
  }
})
